services:
  perch:
    image: ghcr.io/fcwu/perch:latest
    network_mode: host
    restart: unless-stopped
    env_file: .env
    volumes:
      - tmp/workspace:/workspace
      - tmp/perchuser:/home/perchuser
